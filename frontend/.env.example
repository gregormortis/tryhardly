
# ─── Service status banner ────────────────────────────────────────────────────
# Renders a site-wide notice. Leave unset for no banner (the normal state).
# Valid values: payments_paused | maintenance
# See components/ServiceStatusBanner.tsx. Clearing this variable removes the
# banner with no code change.
# NEXT_PUBLIC_SERVICE_STATUS="payments_paused"
